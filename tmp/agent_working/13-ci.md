# Stage 13 — CI wiring & invariants

## Delivered

1. **`INVARIANTS.md`** at repo root. Single page mapping every contract → enforcement path. Drift between the table and the gate is a review-blocking bug.
2. **`.github/workflows/rust-quality.yml`** — new workflow, runs on push + PR + manual:
   - Install GTK4 / libadwaita / WebKitGTK 6 / gettext build deps
   - Pin Rust via `rust-toolchain.toml` (landed Stage 0)
   - `cargo fmt --check`
   - `cargo clippy --workspace --all-targets --locked -- -D warnings`
   - `cargo build --release --workspace --locked`
   - `cargo test --release --workspace --locked`
   - `cargo deny check` (consumes `deny.toml` from Stage 4)
   - `cargo machete` (zero unused deps from Stage 2)
   - i18n POT freshness: re-run `scripts/update-translations.sh`, fail on diff
3. Existing `build-package.yml` left intact — it is the BigLinux packaging hook, not a quality gate; orthogonal.

## Pre-existing (not modified by Stage 13)

- `rust-toolchain.toml` — Stage 0
- `deny.toml` — Stage 4 proposal; **must be committed** alongside the workflow for `cargo deny` to find its config. Commit checklist below.
- `scripts/update-translations.sh` — already in repo; used by the POT-freshness step

## Commit checklist (single PR)

```
git add INVARIANTS.md
git add .github/workflows/rust-quality.yml
git add rust-toolchain.toml
git add deny.toml                          # promote proposal from 04-licensing.md
git add crates/webapps-viewer/Cargo.toml   # Stage 2 dep prune
git add crates/webapps-viewer/src/window/permissions/mod.rs  # Stage 5 H-3 atomic write
git add crates/webapps-manager/src/window/list.rs            # Stage 8 i18n closures
git add po/                                                  # POT + 29 .po regen
git add tmp/agent_working/                                   # pipeline ledger
```

## Deferred (own commits, documented in artifacts)

- Stage 3 Cluster A-D collapses (logging, css, glib markup escape, geometry)
- Stage 4 wildcard intra-dep cleanups; SPDX headers on 5 entry files
- Stage 5 H-1 browser whitelist + test G-1/G-2 (`12-tests.md`)
- Stage 5 M-2 / M-3 hardening
- Stage 7 VM live RSS + flamegraph (Stage 13b once VM CI runner exists)
- Stage 8 `ngettext` migration for the 5 plural sites
- Stage 9 AT-SPI live walk on VM → `09-a11y-vm.md`
- Stage 11 M-11.1 (`webapps-core` rustdoc header) + M-11.2 (root `AGENTS.md`)
- Stage 12 G-3 / G-4 / G-5 / G-6 tests

## Verdict

Pipeline complete through Stage 13. From this commit on, every push runs the seven-step quality gate; every contract in `INVARIANTS.md` has a named enforcement point. The ledger under `tmp/agent_working/` is the historical baseline — future pipeline runs diff against it.
